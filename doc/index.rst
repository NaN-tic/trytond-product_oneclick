Product OneClick Module
#######################

This module adds a wizard to allow the creation of new product (with basic fields) in just One click.

* Use Category's accounts. Default product use account defined in category.
